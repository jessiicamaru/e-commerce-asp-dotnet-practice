# Tasks: Vouchers (part 2 - the screens)

- [ ] T001 Types, the `Voucher` service, and hooks. `Order.quote` and `Order.place` carry `voucherCodes`. URL tests.
- [ ] T002 [US1] `components/checkout/voucher-box` and `useTryVoucher`, wired into `pages/checkout`. `OrderTotals` and `OrderLines` show the vouchers and the discounts. Tests.
- [ ] T003 [US2] [US3] `utils/voucher/describe` (with tests), and the `voucher-form`, `product-picker` and `voucher-page` components.
- [ ] T004 [US2] [US3] `pages/shop-vouchers` and `pages/admin-vouchers`, their routes and their menu entries. Tests for each role.
- [ ] T005 Words (vi and en, the `vouchers` namespace). Then lint, test, type-check and build, the live check through the storefront, and the docs.
