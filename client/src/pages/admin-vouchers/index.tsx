import { VoucherPage } from '@/components/voucher/voucher-page'

/** The platform's vouchers (specs/070): on anything, paid for by the shop; free delivery is here only. */
export function AdminVouchersPage() {
  return <VoucherPage platform />
}
