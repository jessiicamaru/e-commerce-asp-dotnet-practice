info:
  name: a moderator cannot read insights
  type: http
  seq: 9

http:
  method: GET
  url: "{{baseUrl}}/api/products/insights/top-viewed"
  auth:
    type: bearer
    token: "{{modToken}}"

runtime:
  scripts:
    - type: tests
      code: |-
        test("status 403", function() {
          expect(res.status).to.equal(403);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  Staff is not enough: insights are an administrator's.
