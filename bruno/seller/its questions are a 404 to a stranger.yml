info:
  name: its questions are a 404 to a stranger
  type: http
  seq: 80

http:
  method: GET
  url: "{{baseUrl}}/api/products/{{sellerProductId}}/questions"
  auth:
    type: none

runtime:
  scripts:
    - type: tests
      code: |-
        test("status 404", function() {
          expect(res.status).to.equal(404);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  Before specs/081 this answered 200 with the questions of a product nobody could open.
