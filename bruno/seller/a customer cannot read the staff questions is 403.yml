info:
  name: a customer cannot read the staff questions is 403
  type: http
  seq: 76

http:
  method: GET
  url: "{{baseUrl}}/api/questions?hidden=true"
  auth:
    type: bearer
    token: "{{customerToken}}"

runtime:
  scripts:
    - type: tests
      code: |-
        test("status 403", function() {
          expect(res.status).to.equal(403);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  The moderators' view carries the reasons; it is Staff only.
