info:
  name: disabling it again is 409
  type: http
  seq: 64

http:
  method: POST
  url: "{{baseUrl}}/api/vouchers/{{sellerVoucherId}}/disable"
  auth:
    type: bearer
    token: "{{sellerToken}}"

runtime:
  scripts:
    - type: tests
      code: |-
        test("status 409", function() {
          expect(res.status).to.equal(409);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  One guarded statement from Active to Disabled: a second call moves nothing.
