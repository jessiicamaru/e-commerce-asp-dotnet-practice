info:
  name: a seller cannot approve their own product
  type: http
  seq: 15

http:
  method: POST
  url: "{{baseUrl}}/api/products/{{sellerProductId}}/approve"
  auth:
    type: bearer
    token: "{{sellerToken}}"

runtime:
  scripts:
    - type: tests
      code: |-
        test("status 403", function() {
          expect(res.status).to.equal(403);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  Deciding is Staff - a moderator or an administrator.
