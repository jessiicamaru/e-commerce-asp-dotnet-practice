info:
  name: the product is approved again for questions
  type: http
  seq: 67

http:
  method: POST
  url: "{{baseUrl}}/api/products/{{sellerProductId}}/approve"
  auth:
    type: bearer
    token: "{{adminToken}}"

runtime:
  scripts:
    - type: tests
      code: |-
        test("status 200", function() {
          expect(res.status).to.equal(200);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  Back on the shelf after the resubmission above: only a product on sale can be asked about (specs/076).
