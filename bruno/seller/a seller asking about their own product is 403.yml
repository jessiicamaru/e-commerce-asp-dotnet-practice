info:
  name: a seller asking about their own product is 403
  type: http
  seq: 73

http:
  method: POST
  url: "{{baseUrl}}/api/products/{{sellerProductId}}/questions"
  body:
    type: json
    data: |-
      {
        "body": "Is my own product any good?"
      }
  auth:
    type: bearer
    token: "{{sellerToken}}"

runtime:
  scripts:
    - type: tests
      code: |-
        test("status 403", function() {
          expect(res.status).to.equal(403);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  A seller holds Customer too, so this reaches the handler - which refuses a question to one's own shop, in words.
