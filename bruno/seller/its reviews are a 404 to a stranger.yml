info:
  name: its reviews are a 404 to a stranger
  type: http
  seq: 79

http:
  method: GET
  url: "{{baseUrl}}/api/products/{{sellerProductId}}/reviews"
  auth:
    type: none

runtime:
  scripts:
    - type: tests
      code: |-
        test("status 404", function() {
          expect(res.status).to.equal(404);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  The public lookup's rule reaches what hangs on the product: off the shelf, the same 404 as not there.
