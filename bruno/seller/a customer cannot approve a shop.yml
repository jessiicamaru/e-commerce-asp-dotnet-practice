info:
  name: a customer cannot approve a shop
  type: http
  seq: 4

http:
  method: POST
  url: "{{baseUrl}}/api/shop-applications/{{applicationId}}/approve"
  auth:
    type: bearer
    token: "{{customerToken}}"

runtime:
  scripts:
    - type: tests
      code: |-
        test("status 403", function() {
          expect(res.status).to.equal(403);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  Deciding is Staff - an administrator or a moderator.
