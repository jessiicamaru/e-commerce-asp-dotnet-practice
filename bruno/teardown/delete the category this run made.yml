info:
  name: delete the category this run made
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{baseUrl}}/api/categories/{{categoryId}}"
  auth:
    type: bearer
    token: "{{adminToken}}"

runtime:
  scripts:
    - type: tests
      code: |-
        test("gone: 204, or 404 when something earlier already removed it", function() {
          expect([204, 404]).to.include(res.status);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  Last, once the products in it are gone. A 404 here means the category was already removed.
