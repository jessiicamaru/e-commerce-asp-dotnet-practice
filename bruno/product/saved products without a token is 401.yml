info:
  name: saved products without a token is 401
  type: http
  seq: 66

http:
  method: GET
  url: "{{baseUrl}}/api/products/saved"
  auth:
    type: none

runtime:
  scripts:
    - type: tests
      code: |-
        test("status 401", function() {
          expect(res.status).to.equal(401);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  A saved list is somebody's; without a token there is nobody to read it for.
