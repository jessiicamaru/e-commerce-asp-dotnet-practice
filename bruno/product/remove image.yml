info:
  name: remove image
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{baseUrl}}/api/products/{{productId}}/image"
  auth:
    type: bearer
    token: "{{adminToken}}"

runtime:
  scripts:
    - type: tests
      code: |-
        test("status 204", function() {
          expect(res.status).to.equal(204);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  Admin only. Removing an image that is not there is also 204.
