info:
  name: the saved ids hold it once
  type: http
  seq: 63

http:
  method: GET
  url: "{{baseUrl}}/api/products/saved/ids"
  auth:
    type: bearer
    token: "{{customerToken}}"

runtime:
  scripts:
    - type: tests
      code: |-
        test("status 200", function() {
          expect(res.status).to.equal(200);
        });
        test("the product, once", function() {
          expect(res.body.filter(id => id === bru.getVar("productId"))).to.have.length(1);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  The ids alone - what the storefront draws a filled heart from.
