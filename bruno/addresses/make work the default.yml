info:
  name: make work the default
  type: http
  seq: 4

http:
  method: PUT
  url: "{{baseUrl}}/api/addresses/{{workAddressId}}/default"
  auth:
    type: bearer
    token: "{{customerToken}}"

runtime:
  scripts:
    - type: tests
      code: |-
        test("status 204", function() {
          expect(res.status).to.equal(204);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
