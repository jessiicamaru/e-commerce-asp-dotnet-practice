info:
  name: delete work address
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{baseUrl}}/api/addresses/{{workAddressId}}"
  auth:
    type: bearer
    token: "{{customerToken}}"

runtime:
  scripts:
    - type: tests
      code: |-
        test("status 204", function() {
          expect(res.status).to.equal(204);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  Deleting the default promotes another, so a customer with addresses always has exactly one default.
