info:
  name: refresh after logout is 401
  type: http
  seq: 6

http:
  method: POST
  url: "{{baseUrl}}/api/auth/refresh"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("status 401 - the session really ended", function() {
          expect(res.status).to.equal(401);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
