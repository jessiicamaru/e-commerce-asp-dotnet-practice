info:
  name: cart
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}/api/cart/health"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("status 200", function() {
          expect(res.status).to.equal(200);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
