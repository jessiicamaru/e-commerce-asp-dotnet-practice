info:
  name: customer cannot move an order (403)
  type: http
  seq: 7

http:
  method: POST
  url: "{{baseUrl}}/api/orders/{{orderId}}/preparing"
  auth:
    type: bearer
    token: "{{customerToken}}"

runtime:
  scripts:
    - type: tests
      code: |-
        test("status 403", function() {
          expect(res.status).to.equal(403);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
