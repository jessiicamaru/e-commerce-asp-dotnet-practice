info:
  name: applying to sell without a token is 401
  type: http
  seq: 46

http:
  method: POST
  url: "{{baseUrl}}/api/shop-applications"
  body:
    type: json
    data: |-
      {
        "shopName": "Nobody's shop"
      }
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("status 401", function() {
          expect(res.status).to.equal(401);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  An application is made by whoever the token names (specs/044).
