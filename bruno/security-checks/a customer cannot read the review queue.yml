info:
  name: a customer cannot read the review queue
  type: http
  seq: 47

http:
  method: GET
  url: "{{baseUrl}}/api/products/review"
  auth:
    type: bearer
    token: "{{customerToken}}"

runtime:
  scripts:
    - type: tests
      code: |-
        test("status 403", function() {
          expect(res.status).to.equal(403);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  The review queue is Staff's (specs/045).
