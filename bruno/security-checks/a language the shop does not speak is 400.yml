info:
  name: a language the shop does not speak is 400
  type: http
  seq: 17

http:
  method: PUT
  url: "{{baseUrl}}/api/products/{{productId}}/translations/fr"
  body:
    type: json
    data: |-
          {
            "name": "Appareil photo"
          }
  auth:
    type: bearer
    token: "{{adminToken}}"

runtime:
  scripts:
    - type: tests
      code: |-
        test("status 400", function() {
          expect(res.status).to.equal(400);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  Nothing would ever read it, and a row nobody reads is a lie about what the shop offers.
