info:
  name: preparing a sale without a token is 401
  type: http
  seq: 35

http:
  method: POST
  url: "{{baseUrl}}/api/orders/sales/{{orderId}}/preparing"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("status 401", function() {
          expect(res.status).to.equal(401);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
