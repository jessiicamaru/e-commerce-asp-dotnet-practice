info:
  name: a customer cannot read the audit log is 403
  type: http
  seq: 42

http:
  method: GET
  url: "{{baseUrl}}/api/audit"
  auth:
    type: bearer
    token: "{{customerToken}}"

runtime:
  scripts:
    - type: tests
      code: |-
        test("status 403", function() {
          expect(res.status).to.equal(403);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  The audit log is administrators' only (specs/041): it says who did what to whom.
