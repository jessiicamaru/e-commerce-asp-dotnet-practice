info:
  name: my details without a token is 401
  type: http
  seq: 55

http:
  method: GET
  url: "{{baseUrl}}/api/auth/me"
  auth:
    type: none

runtime:
  scripts:
    - type: tests
      code: |-
        test("status 401", function() {
          expect(res.status).to.equal(401);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
