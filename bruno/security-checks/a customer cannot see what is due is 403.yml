info:
  name: a customer cannot see what is due is 403
  type: http
  seq: 37

http:
  method: GET
  url: "{{baseUrl}}/api/orders/payouts/due"
  auth:
    type: bearer
    token: "{{customerToken}}"

runtime:
  scripts:
    - type: tests
      code: |-
        test("status 403", function() {
          expect(res.status).to.equal(403);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  What every seller is owed is staff's business (specs/037).
