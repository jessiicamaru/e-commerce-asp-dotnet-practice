info:
  name: another customer order is 404
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/api/orders/00000000-0000-0000-0000-000000000001"
  auth:
    type: bearer
    token: "{{customerToken}}"

runtime:
  scripts:
    - type: tests
      code: |-
        test("status 404", function() {
          expect(res.status).to.equal(404);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  Unknown and not-yours look the same on purpose, so ids cannot be probed.
