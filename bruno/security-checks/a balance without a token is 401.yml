info:
  name: a balance without a token is 401
  type: http
  seq: 39

http:
  method: GET
  url: "{{baseUrl}}/api/orders/sales/balance"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("status 401", function() {
          expect(res.status).to.equal(401);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  Whose balance comes from the token (specs/037); no token, no balance.
