info:
  name: asking to return a parcel without a token is 401
  type: http
  seq: 58

http:
  method: POST
  url: "{{baseUrl}}/api/orders/00000000-0000-0000-0000-000000000001/shipments/00000000-0000-0000-0000-000000000002/return"
  body:
    type: json
    data: |-
      {
        "reason": "x"
      }
  auth:
    type: none

runtime:
  scripts:
    - type: tests
      code: |-
        test("status 401", function() {
          expect(res.status).to.equal(401);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
