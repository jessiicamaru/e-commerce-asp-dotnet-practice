info:
  name: sales without a token is 401
  type: http
  seq: 33

http:
  method: GET
  url: "{{baseUrl}}/api/orders/sales"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("status 401", function() {
          expect(res.status).to.equal(401);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
