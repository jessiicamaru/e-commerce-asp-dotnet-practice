info:
  name: reviewing without a token is 401
  type: http
  seq: 49

http:
  method: PUT
  url: "{{baseUrl}}/api/products/{{productId}}/reviews/mine"
  body:
    type: json
    data: |-
      {
        "rating": 5
      }
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("status 401", function() {
          expect(res.status).to.equal(401);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  A review is written by whoever the token names (specs/046).
