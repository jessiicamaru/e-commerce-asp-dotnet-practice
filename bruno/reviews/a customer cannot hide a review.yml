info:
  name: a customer cannot hide a review
  type: http
  seq: 7

http:
  method: POST
  url: "{{baseUrl}}/api/reviews/{{reviewId}}/hide"
  body:
    type: json
    data: |-
      {
        "reason": "I changed my mind"
      }
  auth:
    type: bearer
    token: "{{customerToken}}"

runtime:
  scripts:
    - type: tests
      code: |-
        test("status 403", function() {
          expect(res.status).to.equal(403);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  Hiding is Staff.
