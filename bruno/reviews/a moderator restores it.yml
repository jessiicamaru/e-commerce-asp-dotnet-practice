info:
  name: a moderator restores it
  type: http
  seq: 11

http:
  method: POST
  url: "{{baseUrl}}/api/reviews/{{reviewId}}/restore"
  auth:
    type: bearer
    token: "{{adminToken}}"

runtime:
  scripts:
    - type: tests
      code: |-
        test("status 200", function() {
          expect(res.status).to.equal(200);
        });
        test("visible again", function() {
          expect(res.body.hiddenAt).to.equal(null);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  Back on the page and in the average.
