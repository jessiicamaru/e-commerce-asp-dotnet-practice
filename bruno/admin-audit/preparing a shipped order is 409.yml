info:
  name: preparing a shipped order is 409
  type: http
  seq: 7

http:
  method: POST
  url: "{{baseUrl}}/api/orders/{{orderId}}/preparing"
  auth:
    type: bearer
    token: "{{adminToken}}"

runtime:
  scripts:
    - type: tests
      code: |-
        test("status 409", function() {
          expect(res.status).to.equal(409);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  Fulfilment never moves backwards.
