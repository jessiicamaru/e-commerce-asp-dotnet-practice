info:
  name: payment for order
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/payments/{{orderId}}"
  auth:
    type: bearer
    token: "{{adminToken}}"

runtime:
  scripts:
    - type: tests
      code: |-
        test("status 200", function() {
          expect(res.status).to.equal(200);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  `provider` is always `Stub` - no money moves.
