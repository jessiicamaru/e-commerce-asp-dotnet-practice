info:
  name: asking to return it again is 409
  type: http
  seq: 18

http:
  method: POST
  url: "{{baseUrl}}/api/orders/{{orderId}}/shipments/{{shipmentId}}/return"
  body:
    type: json
    data: |-
      {
        "reason": "Again"
      }
  auth:
    type: bearer
    token: "{{customerToken}}"

runtime:
  scripts:
    - type: tests
      code: |-
        test("status 409 - one return per parcel", function() {
          expect(res.status).to.equal(409);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
