info:
  name: a moderator cannot read the email log is 403
  type: http
  seq: 31

http:
  method: GET
  url: "{{baseUrl}}/api/emails"
  auth:
    type: bearer
    token: "{{modToken}}"

runtime:
  scripts:
    - type: tests
      code: |-
        test("status 403 - administrators only, like the words (specs/087)", function() {
          expect(res.status).to.equal(403);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  The list names who was written to about what.
