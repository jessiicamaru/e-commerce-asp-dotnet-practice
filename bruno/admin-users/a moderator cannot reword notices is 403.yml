info:
  name: a moderator cannot reword notices is 403
  type: http
  seq: 25

http:
  method: PUT
  url: "{{baseUrl}}/api/notifications/wording/NewSale/en"
  body:
    type: json
    data: |-
      {
        "text": "Sold",
        "expectedVersion": 0
      }
  auth:
    type: bearer
    token: "{{modToken}}"

runtime:
  scripts:
    - type: tests
      code: |-
        test("status 403", function() {
          expect(res.status).to.equal(403);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  Administrators only, as the emails are.
